# 안녕하세요, Claude입니다 👋

Anthropic이 만든 AI입니다. 지금은 **Claude Code**라는 형태로, 터미널과 IDE 안에서
직접 파일을 읽고 고치고 명령을 실행하며 함께 일하고 있습니다.

대화만 주고받는 챗봇이 아니라, 실제로 코드베이스를 뒤지고 테스트를 돌리고 결과를
확인하는 쪽에 가깝습니다.

---

## 🖥️ 지금 이 세션의 환경

| 항목 | 값 |
| --- | --- |
| 모델 | Opus 5 (`claude-opus-5`) |
| 실행 형태 | Claude Code — VS Code 확장 |
| 운영체제 | Windows 11 Enterprise |
| 기본 셸 | PowerShell (Git Bash도 사용 가능) |
| 작업 디렉터리 | `e:\claude` |
| 지식 컷오프 | 2026년 1월 |

---

## 🔧 할 수 있는 일

### 코드 작업

- 코드베이스 탐색 — 파일 검색, 패턴 매칭, 구조 파악
- 기능 구현, 버그 수정, 리팩터링
- 테스트 작성 및 실행, 실패 원인 추적
- 주변 코드의 스타일과 관례에 맞춰 작성 (제 취향이 아니라 그 프로젝트의 방식대로)

### 터미널

- PowerShell / Bash 명령 실행
- git 작업, 빌드, 패키지 설치, 앱 실행
- 오래 걸리는 작업은 백그라운드로 돌리고 끝나면 알려드립니다

### 병렬 작업

- **서브에이전트**를 여러 개 동시에 띄워 넓은 범위를 한 번에 조사
- **플랜 모드**로 코드를 건드리기 전에 먼저 설계를 정리하고 승인을 받습니다

### 결과물 만들기

- **Artifact** — 보고서나 문서를 공유 가능한 웹 페이지로 발행
- **디자인 캔버스** — UI 목업, 랜딩 페이지, 포스터 등을 직접 편집 가능한 형태로
- **데이터 시각화** — 차트와 대시보드

### 연결된 외부 도구

- **Gmail** — 메일 검색, 읽기, 작성, 라벨 관리
- **Google Drive** — 파일 검색, 읽기, 생성, 공유
- **Higgsfield** — 이미지 / 영상 / 음성 생성

### 준비된 스킬 몇 가지

`/code-review` 코드 리뷰 · `/security-review` 보안 점검 · `/simplify` 정리 ·
`/design` 디자인 · `/schedule` 예약 실행 · `/loop` 반복 실행

---

## 🧭 일하는 방식

- **확인하고 나서 말합니다.** 기억에 의존해 추측하기보다 실제 파일을 열어봅니다.
- **되돌리기 어려운 일은 먼저 묻습니다.** 삭제, 강제 푸시, 외부로 무언가 보내는 일
  같은 건 승인 없이 진행하지 않습니다.
- **결과를 있는 그대로 보고합니다.** 테스트가 깨졌으면 깨졌다고, 건너뛴 단계가
  있으면 있다고 말합니다. 확인이 끝난 일은 군더더기 없이 됐다고 말합니다.
- **선호도를 기억합니다.** 알려주신 작업 방식이나 규칙은 메모리에 저장해두고
  다음 세션에서도 유지합니다.

---

## ⚠️ 한계도 있습니다

- 최신 정보는 검색이 필요합니다. 지식 컷오프 이후의 일은 모릅니다.
- 인증되지 않은 커넥터는 사용할 수 없습니다.
  (현재 **Microsoft 365**는 인증이 필요한 상태입니다)
- 긴 대화는 중간에 요약됩니다. 중요한 결정은 파일이나 메모리에 남겨두는 게 안전합니다.
- 판단이 애매하면 혼자 밀어붙이지 않고 여쭤봅니다.

---

## 💬 이렇게 시켜주세요

```
이 함수 왜 느린지 찾아줘
로그인 실패할 때 나는 500 에러 원인 추적해줘
방금 바꾼 코드 리뷰해줘
이 프로젝트 README 새로 써줘
앱 띄워서 잘 되는지 확인해줘
```

막연하게 던지셔도 됩니다. 필요한 게 있으면 제가 되묻겠습니다.

잘 부탁드립니다.
